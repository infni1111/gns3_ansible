---
# Couche neutre : on ne fait que choisir la traduction propre à l'OS de l'hôte.
# Ajouter un vendeur = ajouter roles/system/tasks/<net_os>.yml, sans toucher aux données.
- name: "system — traduction pour {{ net_os }}"
  ansible.builtin.include_tasks: "{{ net_os }}.yml"
