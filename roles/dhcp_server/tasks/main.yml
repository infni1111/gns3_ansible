---
# Couche neutre : on ne fait que choisir la traduction propre à l'OS de l'hôte.
# Ajouter un vendeur = ajouter roles/dhcp_server/tasks/<net_os>.yml, sans toucher aux données.
- name: "dhcp_server — traduction pour {{ net_os }}"
  ansible.builtin.include_tasks: "{{ net_os }}.yml"
